# Backward-compatible full dependency entry point.
# For Windows local Web/API development and CI, prefer:
#   pip install -r requirements-dev.txt
-c constraints.txt
-r requirements-runtime.txt
-r requirements-optional-heavy.txt
