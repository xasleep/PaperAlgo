# Minimal Windows local runtime for the FastAPI control plane, WebUI serving,
# SQLite worker, remote-provider pipeline scripts, and cost/accounting helpers.
-c constraints.txt

openai>=1.65.4
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
pydantic>=2.8.0
starlette>=0.40.0
tiktoken>=0.9.0
tqdm>=4.66.0
